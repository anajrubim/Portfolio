<%- include('../partials/header', { title: 'Dashboard' }) %>
<%- include('../partials/nav', { activePage: 'dashboard' }) %>

<section class="dashboard">
    <div class="title">
        <span>Dashboard de Estatísticas</span>
    </div>

    <div class="dashboard-stats">
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-book"></i>
                </div>
                <div class="stat-info">
                    <h3><%= stats.totalDisciplinas %></h3>
                    <p>Total de Disciplinas</p>
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                <div class="stat-info">
                    <h3><%= stats.disciplinasConcluidas %></h3>
                    <p>Disciplinas Concluídas</p>
                </div>
            </div>

            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-project-diagram"></i>
                </div>
                <div class="stat-info">
                    <h3><%= stats.projetosConcluidos %> / <%= stats.totalProjetos %></h3>
                    <p>Projetos Concluídos</p>
                </div>
            </div>
        </div>

        <div class="tech-section">
            <h3>💻 Tecnologias Mais Usadas</h3>
            <div class="tech-tags">
                <% stats.tecnologiasPopulares.forEach(([tech, count]) => { %>
                    <span class="tech-tag"><%= tech %>: <%= count %> projeto(s)</span>
                <% }); %>
            </div>
        </div>

        <div class="profile-summary">
            <h3>📊 Resumo do Perfil</h3>
            <div class="summary-grid">
                <div class="summary-item">
                    <strong>Nome:</strong> <%= estudante.nomeCompleto %>
                </div>
                <div class="summary-item">
                    <strong>Curso:</strong> <%= estudante.curso %>
                </div>
                <div class="summary-item">
                    <strong>Instituição:</strong> <%= estudante.instituicao %>
                </div>
                <div class="summary-item">
                    <strong>Ano de Ingresso:</strong> <%= estudante.anoIngresso %>
                </div>
            </div>
        </div>

        <div class="progress-section">
            <h3>📈 Progresso Acadêmico</h3>
            <div class="progress-grid">
                <div class="progress-item">
                    <label>Disciplinas Concluídas</label>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: <%= (stats.disciplinasConcluidas / stats.totalDisciplinas * 100).toFixed(0) %>%"></div>
                    </div>
                    <span><%= (stats.disciplinasConcluidas / stats.totalDisciplinas * 100).toFixed(0) %>%</span>
                </div>
                
                <div class="progress-item">
                    <label>Projetos Concluídos</label>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: <%= (stats.projetosConcluidos / stats.totalProjetos * 100).toFixed(0) %>%"></div>
                    </div>
                    <span><%= (stats.projetosConcluidos / stats.totalProjetos * 100).toFixed(0) %>%</span>
                </div>
            </div>
        </div>
    </div>
</section>

<%- include('../partials/footer') %>
